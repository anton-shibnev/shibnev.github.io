import React from 'react';
import { Outer } from '../../components';
import { Title } from '../../style/helpers/typography';

export const Works = () => {
  return (
    <main>
      <Outer>
        <Title>Works</Title>
      </Outer>
    </main>
  );
};
